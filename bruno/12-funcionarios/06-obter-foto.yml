info:
  name: Obter foto
  type: http
  seq: 6
http:
  method: GET
  url: '{{base_url_api}}/IntegracaoExterna/Funcionarios/Fotos?funcionarioId={{funcionario_id}}'
  headers:
    - name: Accept
      value: application/json
    - name: secullumidbancoselecionado
      value: '{{selected_bank_id}}'
  params:
    - name: funcionarioId
      value: '{{funcionario_id}}'
      type: query
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
docs: 'Tag: Funcionários. OperationId: ObterFotoAsync'
